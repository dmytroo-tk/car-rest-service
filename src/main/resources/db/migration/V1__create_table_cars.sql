CREATE TABLE IF NOT EXISTS cars
(
    object_id           VARCHAR PRIMARY KEY,
    brand               VARCHAR,
    year_of_manufacture INTEGER,
    model               VARCHAR,
    body_type           VARCHAR
);