spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/car-rest-service
    username: postgres
    password: baloo
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

cars-file-path: src/main/resources/file.csv
